controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

auth:
    path: /auth
    methods: ['POST']

api_logout:
    path: /api/logout
    methods: ['POST']
    # controller: App\Controller\LogoutController::logout

# api_refresh_token:
#     path: /api/token/refresh 
